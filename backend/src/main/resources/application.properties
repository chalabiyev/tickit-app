# ?????????????????????????????????????????????????????????????
#  eticksystem ? Base configuration
#  ???????? ??????, ?????????? ?? ???????.
#  ????? ?????? ????? ????????? ? ??? ????????.
# ?????????????????????????????????????????????????????????????

spring.application.name=eticksystem
server.port=8080

# Multipart (???????? ??????)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
server.tomcat.max-swallow-size=-1
server.tomcat.max-http-form-post-size=-1

# ?????? ? GlobalExceptionHandler ??? ????????? JSON, Spring ?? ?????? ????????? ????
server.error.include-message=never
server.error.include-stacktrace=never

# JWT ? ????? ????? ?????? ????????? ????? (24 ????)
security.jwt.expiration-ms=86400000

# Mail ? ????? ????????? SMTP ?????????
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT secret ? ?????? ??? ??????? ??? ???????
# ? prod ???????????????? ????? application-prod.properties + env var JWT_SECRET
security.jwt.secret=${JWT_SECRET:dev_local_secret_key_minimum_32_chars!!}

# Mail ? ????????? ???????? (???????????????? ? dev/prod ????????)
spring.mail.host=${MAIL_HOST:mail.eticksystem.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:no-reply@eticksystem.com}
spring.mail.password=${MAIL_PASSWORD:pLs5Wt4O0k6C6c2ZlX6mZGv5V}
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:mail.eticksystem.com}

# MongoDB ? ?????? ??? ???????
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/eticksystem}

# CORS ? ?????? ??? ???????
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}